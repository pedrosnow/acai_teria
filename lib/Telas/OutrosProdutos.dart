import 'package:flutter/material.dart';

class OutrosProdutos extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Outros Produtos'),
      ),
    );
  }
}
